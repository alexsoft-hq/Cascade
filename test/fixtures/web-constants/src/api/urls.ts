// The module a sibling imports its paths from. Nothing here calls anything: it
// is the constant table, which is where most TypeScript frontends keep one.
export const COMMENTS_URL = '/board-service/api/v1/comments'

export const Paths = {
  boards: '/board-service/api/v1/boards',
}
